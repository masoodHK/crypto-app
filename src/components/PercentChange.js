import { Text } from 'react-native'
import styled from 'styled-components'

const PercentChange = styled(Text)`
    color: ${props => props.color};
    font-weight: bold;
`
export default PercentChange